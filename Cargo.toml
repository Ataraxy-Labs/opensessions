[workspace]
members = [
    "apps/tui-rs",
    "apps/server-rs",
    "packages/runtime-rs",
    "packages/ratatui-query-rs",
    "packages/query-core-rs",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"
incremental = false

[profile.release-dev]
inherits = "release"
opt-level = 3
strip = false
debug = "line-tables-only"
